# services/text_analysis.py
def analyze_text(text):
    # Replace this with the actual call to Yerkem’s function when available
    keywords = ["example", "keywords", "from", "text"]  # Mock data
    sentences = text.split(". ")  # Simple sentence splitting; replace with actual processing logic
    return keywords, sentences
